llm:
  provider: "openai"
  model: "gpt-4o"
  api_key: ""
  temperature: 0.0
  max_tokens: 4096

embedding:
  provider: "openai"
  model: "text-embedding-ada-002"
  dimensions: 1536
  api_key: ""

vector_store:
  provider: "chroma"
  persist_directory: "data/db/chroma"

retrieval:
  top_k: 10
  hybrid: true

rerank:
  enabled: false
  provider: "none"
  model: ""
  top_k: 5

evaluation:
  backends: ["custom"]

observability:
  log_level: "INFO"
  traces_file: "logs/traces.jsonl"
